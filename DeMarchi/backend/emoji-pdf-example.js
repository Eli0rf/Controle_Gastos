const PDFDocument = require('pdfkit');const fs = require('fs');const path = require('path');const doc = new PDFDocument();const outputPath = path.join(__dirname, 'emoji-example.pdf');doc.pipe(fs.createWriteStream(outputPath));// Registra a fonte NotoSans (Unicode/Emoji)doc.registerFont('NotoSans', path.join(__dirname, 'fonts', 'NotoSans-Regular.ttf'));// Usa a fonte registradadoc.font('NotoSans').fontSize(24).text('Relatório Mensal 📊', { align: 'center' });doc.moveDown();doc.fontSize(18).text('Gastos Empresariais 💼', { align: 'left' });doc.moveDown();doc.fontSize(14).text('Nota Fiscal: Sim 📄  |  Não ❌', { align: 'left' });doc.moveDown();doc.fontSize(14).text('Obrigado por usar o Controle de Gastos! 🚀', { align: 'center' });doc.end();console.log('PDF gerado em:', outputPath);